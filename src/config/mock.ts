/**
 * Demo mode flag.
 *
 * Set to true to run TAKUMI entirely from local mock data without a Canopy node.
 * Set to false to use the existing on-chain RPC implementation exactly as-is.
 */
export const USE_MOCK=false;
